# Runtime dependencies for humanoid_ps4_control
# Install with: pip install -r requirements.txt

# PS4 / joystick input and keyboard fallback (cross-platform: Windows, macOS, Linux, Raspberry Pi)
pygame>=2.1.0

# Serial communication with RTrobot controller board
pyserial>=3.5

# ZMP preview controller and IK trajectory math
numpy>=1.24

# BNO055 IMU feedback on Raspberry Pi. Required only when using --imu-balance.
adafruit-circuitpython-bno055>=5.4.0

# ADS1115 ADC for FSR foot-load feedback. Required only when using sensor_feedback.
adafruit-circuitpython-ads1x15>=2.4.0

# Standard library only for XML parsing, CSV, threading
